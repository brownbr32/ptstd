./name-runner given-names
   1. takes given-names.in as input,
   2. makes requests to standards
   3. outputs as given-names.out in *.csv format with:
   4. name, 1 or 0 (present, not present)
   5. the final line will contain the total passed requests and the total request count.

./name-runner surnames
   * does same as above, but for:
      surnames.in
      surnames.out
